apiVersion: v1
kind: Service
metadata:
  labels:
    app: exploreca.com
  name: exploreca-svc
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: exploreca.com
  type: ClusterIP
status:
  loadBalancer: {}
